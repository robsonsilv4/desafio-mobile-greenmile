name: greenmile_challenge
description: Desafio Mobile - GreenMile.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  http: ^0.12.2
  json_annotation: ^3.0.1
  intl: ^0.16.1
  meta: ^1.1.8
  equatable: ^1.2.3
  bloc: ^6.0.1
  flutter_bloc: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.10.0
  json_serializable: ^3.4.0

flutter:
  uses-material-design: true
